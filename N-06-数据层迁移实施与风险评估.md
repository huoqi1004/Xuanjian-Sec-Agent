# N-06 数据层迁移（MySQL 8 / PostgreSQL + DAO）实施步骤与风险评估

> 版本：v1.0 ｜ 日期：2026-08-09
> 关联文档：[NEXT_ITERATIONS.md](./NEXT_ITERATIONS.md)（N-06）、[ROADMAP.md](./ROADMAP.md)
> 前置依赖：N-02（租户/对象级权限）已完成、N-24（测试覆盖补全）未完成，本方案依赖其回归保障

---

## 一、现状基线（基于当前代码库实测）

### 1.1 存储架构
| 项目 | 现状 | 位置 |
|------|------|------|
| 数据访问层 | 自研内存 shim（非 SQLite/MySQL/PG） | `server/db/database.js`（888 行） |
| 持久化 | JSON 文件防抖落盘（800ms），默认 `data/security.db` | [database.js](file:///e:/迅雷下载/xuanjian-security-agent/server/db/database.js) |
| 表结构定义 | 24 张 `CREATE TABLE` + 4 张迁移新增表 ≈ **28 张表** | `server/db/init.js` |
| 迁移机制 | JS 数据变更式迁移（`NNN_name.js`），`schema_migrations` 表记录 | `server/db/migrations/index.js` |
| 数据初始化 | `insertDefaultData()`：admin 账号、角色权限、基线策略、等保数据、威胁情报等 | `server/db/init.js` |
| 调用规模 | **41 个文件**直接 `require('../db/database')`，**282 处** `db.prepare()` | 全库 |

### 1.2 shim 能力边界（迁移到真实 SQL 时必须重写的差异点）
| shim 能力/限制 | 说明 | 影响 |
|----------------|------|------|
| ✅ `SELECT *` / 单列投影（`SELECT key, value`） | 多列投影不支持（如 `SELECT id, username` 返回 undefined） | 全库 SELECT 需统一为 `*` 或由 DAO 投影 |
| ✅ 单表 `WHERE col = ?` / `WHERE col = '字面量'` | 仅支持**单个**等值条件 | 多条件/范围/IN 必须改写 |
| ✅ 单表 `ORDER BY` + `LIMIT ? OFFSET ?` | 支持 | 可直接迁移 |
| ✅ 单 JOIN（`LEFT JOIN ... ON a.x = b.y`）+ WHERE | JOIN 解析依赖正则，多 JOIN/复合 ON 不支持 | 复杂查询需拆解 |
| ✅ `COUNT(*)`（单条件） | 支持 | 可直接迁移 |
| ✅ INSERT / UPDATE / DELETE | INSERT **参数按位置映射到全部列**，混合字面量会错位（N-01 已踩坑） | 必须全部改参数化 SQL |
| ❌ 事务 | 无事务概念 | 多步写入需引入事务边界 |
| ❌ 索引 | `CREATE INDEX` 为空操作，全表扫描 | SQL 过滤迁移后必须补索引 |
| ❌ 多条件 AND/OR、IN、范围比较、子查询、GROUP BY | 均不支持 | 服务层"全量取+内存过滤"模式需改为 SQL 过滤 |
| ❌ 类型系统 | 无类型约束，JSON 字符串直存 | 需定义 DDL 类型、时间格式、JSON 列策略 |
| ⚠️ `_rawTable` / `_deleteRows` / `saveDb` / `resetForTest` | shim 特有 API，真实库无对应物 | 需 DAO 封装替换（约 15 处调用） |

### 1.3 服务层现状模式（迁移最大工作量来源）
因 shim 查询能力有限，**N-02 之前的列表过滤普遍采用"全量 SELECT + JS 内存过滤"**，例如：
- `scanService.getTasks`：全量 JOIN 后 `inOrg()` 内存过滤
- `djppService.getTasks` / `getReports`：同上
- `virusService.getRecords`：同上
- `playbookService.listPlaybooks`：全量 + 内存过滤
- `routes/reports.js /list`：全量 + `filterReportsByOrg`

→ 这些逻辑在迁移后需下推为 SQL 条件（`WHERE created_by IN (组织用户集)` 或 `JOIN users ON org_id`），**是本次迁移的核心改造面**。

### 1.4 部署现状
[docker-compose.yml](file:///e:/迅雷下载/xuanjian-security-agent/docker-compose.yml)：server 服务 `DB_PATH=/app/data/security.db`，挂载 `server-data` volume；**无任何数据库容器**。切换真实库后需新增 mysql/pg 服务并调整 env。

---

## 二、目标与验收标准（沿用 NEXT_ITERATIONS N-06）

- **目标**：引入 DAO 抽象层；SQLite 保持开发默认，`DB_DRIVER=sqlite|mysql|pg` 切换真实数据库；Schema 迁移（migrations）对齐真实 SQL。
- **验收**：
  1. 空库/存量库迁移均可执行；
  2. 同一套业务代码在 SQLite 与 MySQL **双跑测试全部通过**（Jest 80 例 + Pytest 11 例不回归）；
  3. `npm audit` 保持 0 漏洞；
  4. 容器化部署可一键拉起真实数据库并完成迁移。

---

## 三、技术选型建议

| 项 | 建议 | 理由 |
|----|------|------|
| SQLite 驱动 | `better-sqlite3`（同步 API，与现服务风格一致） | 开发默认、零运维 |
| MySQL 驱动 | `mysql2/promise` | 生产可选，事务/Pool 成熟 |
| PG 驱动 | `pg`（Pool） | 生产可选，若用 PG 需注意 `$1` 占位符差异 |
| DAO 形态 | **Repository 模式 + 单例数据访问对象**（`server/db/drivers/` + `server/db/dao/`） | 薄封装，保持 `db.prepare().get/all/run` 调用面兼容，服务层改动最小化 |
| 迁移形态 | 保留 `runMigrations()` 执行器，迁移体改为**驱动无关的 DDL/DML 描述或双写 SQL** | 沿用现有 7 个迁移，扩展而非推翻 |
| 查询改造策略 | 分阶段下推 SQL（见第四节），不一次性重写 | 控制回归面 |

**关键决策：兼容层优先。** 新 DAO 对外保持 `db.prepare(sql).get/all/run` + `db.exec` 签名，使 282 处调用中**纯 SQL 可直接跑通**的（`*`+单条件+单JOIN）无需改动，只有 shim 特有用法（`_rawTable`、多列投影、内存过滤）需要改造。

---

## 四、详细实施步骤（分 5 个阶段，每阶段含验证）

### Phase A：驱动抽象层（纯新增，无业务改动）
1. 新建 `server/db/drivers/`：
   - `base.js`：统一接口契约 `{ exec(sql), prepare(sql): { get/all/run }, close() }`
   - `sqlite.js`：`better-sqlite3` 实现（`DB_DRIVER=sqlite`，默认）
   - `mysql.js`：`mysql2/promise` 实现（占位符 `?`→`?`，异步转同步由 DAO 包装或返回 Promise）
   - `pg.js`：`pg` 实现（占位符 `?`→`$n` 转换层）
2. 新建 `server/db/dao.js`：单例工厂，按 `process.env.DB_DRIVER` 选择驱动；暴露 `getDb()` 保持旧签名；**迁移期提供 shim 兼容代理**（未改造的 `_rawTable`/内存过滤调用走 shim 或报错清单）。
3. `config/index.js` 增加：`db.driver`、`db.host/port/user/password/database`（从 env 读取）。
4. `.env.example` 补充 `DB_DRIVER`、`DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME`。
5. **验证**：`npm test` 80 例全绿（默认 sqlite 路径零回归）。

### Phase B：Schema DDL 落地 + 迁移机制升级
1. 编写 `server/db/schema.sql`（SQLite 方言）与 `server/db/schema.mysql.sql` / `schema.pg.sql`：28 张表完整 DDL，含：
   - 主键/唯一约束/外键（对齐 `uniqueConstraints` 与 `defaultValues`）
   - **全部列表过滤列的索引**：`scan_tasks(created_by)`、`virus_scan_records(uploaded_by)`、`djpp_tasks(created_by)`、`reports(generated_by,type)`、`playbooks(created_by)`、`djpp_results(task_id)`、`scan_results(task_id)`、`users(org_id)` 等
   - `created_at` 等时间列统一 `DATETIME`/`TIMESTAMP`
2. 迁移执行器 `runMigrations()` 改造：
   - 建 `schema_migrations`（真实 DDL）
   - 迁移文件改为**双格式**：保留 JS 迁移（兼容既有 001-007），新增 SQL 迁移目录 `migrations/sql/`（按版本号排序执行）
   - `DB_DRIVER != sqlite` 时跳过 JS 数据变更迁移，改执行 SQL 文件 + seed SQL
3. **seed 迁移**：将 `insertDefaultData()` 的幂等逻辑改写为 SQL（`INSERT ... ON DUPLICATE KEY` / `ON CONFLICT DO NOTHING`），保证 admin/角色/基线/等保/情报可重建。
4. **验证**：空库 `DB_DRIVER=sqlite` 全量迁移 → `GET /api/health` 各表行数正确；admin 可登录。

### Phase C：服务层查询下推（核心改造，按表分批）
> 原则：**一批表一个 PR/提交**，每批跑全量测试 + 双跑。优先级按业务敏感度从低到高。

| 批次 | 改造对象 | 改造内容 |
|------|----------|----------|
| C1 | 配置/字典类 | `sys_config`、`djpp_levels/categories/checks`、`organizations`：纯读，直改 SQL |
| C2 | 扫描域 | `scan_tasks`、`scan_results`：`getTasks/getTaskDetail/deleteTask` 的 JOIN+WHERE 参数化；列表过滤下推为 `WHERE created_by IN (组织用户)` |
| C3 | 查杀域 | `virus_hashes`、`virus_scan_records`：`getRecords/getScanHistory` 下推 `uploaded_by` 组织过滤 |
| C4 | 等保域 | `djpp_tasks`、`djpp_results`、`reports`（djpp 部分）：列表/详情下推 |
| C5 | 剧本域 | `playbooks`、审批表：`listPlaybooks` 下推 `created_by` 组织过滤 |
| C6 | 报告域 | `reports` 全量：`routes/reports.js` 的 list/detail/delete 下推 |
| C7 | 运维/审计 | `audit_logs`、`threat_intel`、`alert_records`、`edge_devices`、`device_commands`、`auto_policies`、`action_logs`、`policy_approvals`、`baseline_*`、`chat_history` |
| C8 | 收尾清理 | 移除 `tenantHelpers` 内存过滤的临时兜底（保留 `isOwner` 对象级判断）；删除 shim 特有 `_rawTable` 依赖（约 15 处） |

**每批验证**：`npm test`（80 例）+ `DB_DRIVER=sqlite` 冒烟 + 对改造表执行真实 API 冒烟（登录→列表→详情→删除）。

### Phase D：MySQL / PG 双跑验证
1. `docker-compose.yml` 新增 `mysql:8` 服务（`MYSQL_ROOT_PASSWORD`、`MYSQL_DATABASE=xuanjian`），server 增加 `DB_DRIVER=mysql` + 连接配置。
2. 新建 `server/test/db-adapters.test.js`：同一套 CRUD 断言在 sqlite/mysql 双驱动下执行（CI 增加一个 `mysql` 服务 job）。
3. **验证**：`DB_DRIVER=mysql` 下 Jest 全绿；空库与"从 sqlite 导出的存量库"两条路径迁移成功；admin 全流程可用。
4. PG 方言按需补跑（占位符转换单测）。

### Phase E：部署切换与收尾
1. docker-compose 生产 profile：server + ai-service + mysql + （可选）备份容器。
2. 备份脚本 `scripts/backup.js` 升级：从"拷贝 JSON 文件"改为"真实库 dump"（`mysqldump` / `sqlite3 .backup`）。
3. N-10（时序归档）联动：告警/态势历史表接入真实库后保留策略走 SQL 删除。
4. 文档与勾选：更新 `NEXT_ITERATIONS.md` 状态为 ✅ 已完成。

---

## 五、风险清单与缓解措施

| # | 风险 | 等级 | 触发场景 | 影响 | 缓解措施 |
|---|------|------|----------|------|----------|
| R1 | **查询语义漂移**：shim 的"全量+内存过滤"改为 SQL 后分页/排序结果不一致 | 高 | C2-C8 任意批次 | 列表数据错乱/越权泄露 | 每批双跑断言（sqlite↔mysql 同断言）；改造前后对同一数据集 diff 分页结果 |
| R2 | **组织隔离失效**：SQL 下推遗漏 `inOrg` 过滤条件 | 高 | C2-C5 | 跨租户数据泄露（N-02 回归） | 先补 `tenant.test.js` 的 SQL 版断言；每批跑租户测试；DAO 层强制注入 tenant 参数 |
| R3 | **shim 特有 API 残留**：`_rawTable`/多列投影未被替换 | 中 | C8 收尾 | 运行时 undefined 异常 | 改造前做静态扫描清单（15 处）；DAO 提供 `list(table)`/`count(table)` 替代 |
| R4 | **事务缺失引入脏数据**：多步写入（如 DJPP 任务+结果+报告）中途失败 | 中 | 真实库启用后 | 部分写入 | 对原子操作引入 `db.transaction()` 包装（DAO 提供）；C4 等保域优先落地 |
| R5 | **类型/时间不一致**：JSON 列、`datetime('now')`、空串 vs NULL | 中 | 迁移存量数据 | 查询/统计异常 | schema.sql 明确 `JSON`/`DATETIME`/默认值；迁移脚本做数据清洗映射 |
| R6 | **迁移幂等性破坏**：JS 迁移（001-007）与 SQL 迁移在真实库冲突 | 中 | 空库首次迁移 | 启动失败 | 迁移执行器按 `DB_DRIVER` 分支；schema_migrations 唯一约束防重放 |
| R7 | **INSERT 占位符错位回归**（shim 历史 bug 模式） | 中 | C 批改造 | 列数据错位 | 参数化 SQL 全覆盖；database.test.js 增加"参数数=列数"断言 |
| R8 | **密码/密钥配置泄漏**：DB 凭据进镜像/仓库 | 中 | Phase D/E | 安全事件 | 凭据只走 env/secret（`.env`、compose env_file），不入代码与 Dockerfile |
| R9 | **上线切换数据丢失**：内存 JSON 与真实库并行期间双写不一致 | 高 | Phase D 切换窗口 | 数据丢失 | 先只读验证（真实库只读+比对）→ 短停维护窗口单次全量迁移 → 校验行数 → 切换 |
| R10 | **better-sqlite3 原生编译失败**（Windows/CI 环境） | 低 | 安装阶段 | 构建失败 | 提供 `--build-from-source` 回退或 `node:sqlite`（Node 22+ 内置）备选 |
| R11 | **CI 双跑时长上升** | 低 | Phase D 后 | CI 变慢 | mysql job 独立并行；仅跑核心测试集（api/tenant/scanControl/database） |
| R12 | **PG 占位符差异遗漏** | 低 | Phase D-PG | 查询报错 | 占位符转换层单测覆盖 `?`→`$n`；PG 至少跑 1 次集成冒烟 |

---

## 六、回滚方案

1. **代码级**：每批改造独立提交，`git revert` 单批即可恢复；DAO 兼容层保留 shim 分支（`DB_DRIVER=shim` 保留 2 个版本窗口）。
2. **数据级**：切换前对 JSON 文件做快照备份（现有 `scripts/backup.js`）；迁移脚本支持 `down()` 或重建库重放（空库重迁移）。
3. **运行时**：环境变量回切 `DB_DRIVER=shim`（或 sqlite）即可回到原路径，无需停机。
4. **演练**：Phase D 前完成一次"备份→清库→恢复→校验"演练（联动 N-11）。

---

## 七、工作量与里程碑建议

> 说明：不承诺具体天数，按批次划分里程碑；每批次独立可交付、可回归。

- **M1**：驱动抽象层 + schema.sql + 迁移器升级（Phase A+B）
- **M2**：C1-C4 查询下推 + 每批双跑（Phase C 前半）
- **M3**：C5-C8 查询下推 + shim 特有 API 清除（Phase C 后半）
- **M4**：MySQL 容器化 + 双跑测试 + 备份升级（Phase D+E）

**关键路径**：Phase A → B（依赖：N-24 测试覆盖先行，保障回归）→ C（最大工作量）→ D → E。
**建议并行**：Phase A/B 可与 N-07（BullMQ 接入）、N-08（OTel）并行——不共享存储改动面。

---

## 八、结论

N-06 是当前**改造面最大、回归风险最高**的 P1 项：涉及 41 个文件、282 处查询调用、28 张表，且依赖 N-02 的组织过滤正确性。建议**严格按"兼容层优先 + 分批下推 + 双跑断言 + 单批可回滚"策略**推进，并将 N-24（测试覆盖 ≥70%）作为前置条件先行落地，以保障每批改造的可验证性。
